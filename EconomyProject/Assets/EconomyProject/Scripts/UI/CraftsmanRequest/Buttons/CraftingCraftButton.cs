using UnityEngine;

namespace Assets.EconomyProject.Scripts.UI.CraftsmanRequest.Buttons
{
    public class CraftingCraftButton : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
